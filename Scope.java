public class Scope {
    public static void main(String[] args) {
        //scope is the region of the program where a variable is defined and can be accessed
        //there are three types of scope in java: local scope, instance scope, and static scope

        //local scope: variables declared inside a method or block are called local variables and they have local scope
        //local variables can only be accessed within the method or block where they are declared

        //instance scope: variables declared inside a class but 
    }
}
